---
import { Image } from 'astro:assets'

const {
    filename,
    clip,
    class: classes,
    ...attrs
} = Astro.props
---
<Image
    src={ filename }
    alt={ '' }
    width="1600"
    height="1600"
    quality="100"
    draggable="false"
    class:list={[
        'object-cover aspect-[3/4] h-[520px] sm:h-[572px]',
        clip === 'y' && 'clip-path-y',
        classes
    ]}
    { ...attrs }
/>