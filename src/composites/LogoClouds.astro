---
import Wrapper from '../layouts/Wrapper.astro'
import { _padY } from '../scripts/snips'
import DashedBox from '../atoms/DashedBox.astro'
import SectionTitle from '../blocks/SectionTitle.astro'
import Cloud from '../atoms/Cloud.astro'

const
    {
        blok
    } = Astro.props

---
<section class:list={[ ..._padY() ]}>
    <Wrapper inner>
        <DashedBox class:list="relative">
            <div class:list={[ 'px-4 sm:px-8 lg:px-12 space-y-12 lg:space-y-20', ..._padY() ]}>
                <SectionTitle
                    title={ blok.title }
                    class:list="opacity-60"
                    centered
                />
                <ul class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-20 gap-y-14 lg:gap-x-32 lg:gap-y-20">
                    { blok.logos.map((entry: any) => (
                        <li>
                            <Cloud filename={ entry.image.filename }/>
                        </li>
                    )) }
                </ul>
            </div>
        </DashedBox>
    </Wrapper>
</section>