---
import Anchor from '../atoms/Anchor.astro'
import Button from '../atoms/Button.astro'

const { links } = Astro.props
const isDev = import.meta.env.DEV
---

<ul class='flex items-center text-white/65'>
	{
		links.map((link: any) => (
			<li class='group'>
				<Anchor
					href={`/${
						link.link.story.full_slug === 'home'
							? ''
							: link.link.story.full_slug
					}`}
					hover='white'>
					<Button
						label={link.title}
						color={link.color}
						titleCase={link.is_titlecase}
						class:list='group-first:!pl-0 lg:group-first:!pl-3'
					/>
				</Anchor>
			</li>
		))
	}
	{
		isDev && (
			<li class='group'>
				<Anchor
					href={`/products-filtered`}
					hover='white'>
					<Button
						label={'Products Filtered'}
						color={'plain'}
						titleCase={true}
						class:list='group-first:!pl-0 lg:group-first:!pl-3'
					/>
				</Anchor>
			</li>
		)
	}
</ul>
