---
const {
    class: classes,
    outer = true,
    centered = true,
    padX = true,
    inner,
    padY = '',
    ...props
} = Astro.props
---
<div
    { ...props }
    class:list={[
        (outer && !inner) && 'max-w-5xl lg:max-w-screen-xl',
        centered && 'mx-auto',
        padX && 'px-3',
        inner && 'max-w-4xl lg:max-w-[70rem]',
        padY === 'y' && 'py-10 lg:py-20',
        classes
    ]}>
    <slot/>
</div>